# Deployment

This repository intentionally does not document a specific host, LXC, network,
reverse proxy, secret store or persistent production state.

Production deployment is maintained in the operator's private Proxmox
documentation. Review the MCP Gateway runbook there before installing the
artifacts in this directory. The checkout must be obtained from this repository;
do not copy a local workspace into a server.
